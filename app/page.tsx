import Home from "@/components/Home";
import React from "react";

export const metadata = {
  title: "HomePage - Lawconnect",
};

const getProducts = () => {
  return (
    <div>
      <Home />
    </div>
  );
};

export default getProducts;
